# roles/k8s-backend/defaults/main.yaml
---
# Configuration par défaut
workspace_path: "/tmp/k8s-backend-workspace"
backend_namespace: "backend"
backend_manifests_source: "backend/k8s-manifests.yaml"

# Configuration des images Docker
backend_image: "your-registry/backend-api:latest"

# Configuration réseau
backend_domain: "api.votre-domaine.com"
